@model kauaicapstone.Models.Legend

@{
    ViewData["Title"] = "Create";
}

<h1>add new legend</h1>

<h4>where can you see this legend from?</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-controller="ViewLocations" asp-for="LegendViewLocations" class="control-label"> </label>
                <select  asp-for="Title" class="form-control">
                <option asp-action="Create" class="text-danger" asp-route-id="@Model">i can't find the location</option>
                    </select>
            </div>
            <div class="form-group">
                <input type="submit" value="submit" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
