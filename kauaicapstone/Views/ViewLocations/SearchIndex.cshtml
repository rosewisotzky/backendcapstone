@model IEnumerable<kauaicapstone.Models.ViewLocation>

@{
    ViewData["Title"] = "SearchIndex";
}

<h1>search results</h1>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ViewPointAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ViewPointAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ViewLocationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ViewLocationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ViewLocationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
